/**
 * Navigation logic removed. 
 * User is starting over on their own.
 */

export function initTVControls() {
    console.log('TV Controls placeholder');
}

export const customNavigation = {
    focusElement: () => { },
    makeFocusable: () => { },
    refresh: () => { }
};
